def lingxue(attr):
    attr.physical_critical_strike_gain += 0.03
    attr.physical_attack_power_gain += 0.1
    attr.physical_critical_effect_gain += 0.15


def jingyu(attr):
    attr.strength_gain += 0.03
    attr.strain_gain += 0.02
    attr.physical_overcome_gain += 0.2
    attr.physical_critical_strike_gain += 0.05


def jianchun(attr):
    attr.physical_critical_strike_gain += 0.03
    attr.strain_gain += 0.02
    attr.physical_critical_effect_gain += 0.15
    attr.physical_critical_strike_gain += 0.05


def badao(attr):
    attr.physical_attack_power_gain += 0.05
    attr.strain_gain += 0.02
    attr.physical_overcome_gain += 0.1
    attr.physical_critical_strike_gain += 0.05
